Goal: Set up kafka locally, define a topic structure, implement producer and consumer services, and validate, end to end event flow. 

First consumer to build:
1. Telemetry Consumer: Reads telemetry events from the kafka topic and:
prints them
logs them
verfies event flow is working end to end. 

This will aim to be our first POC consumer. 

Questions to get answer before build:
1. So what is kafka I understand EDA(event-driven architecture) as well as publisher consumer model. Topics. But now what is kafka is it a framework that makes it easy?

2. Should we use docker for kafka and what do we use docker for in this project?

3. What is the benfit of using docker?

4. What are some early questions I should answer and consider before prompting and building?

